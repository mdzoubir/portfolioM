{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile-sass": "node-sass -w sass -o css",
    "prefix": "postcss css/main.css --use=autoprefixer --map=false -o css/main.css",
    "build": "npm run compile-sass &&  npm run prefix "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.14.1"
  },
  "dependencies": {
    "autoprefixer": "^9.8.6",
    "postcss-cli": "^7.1.1"
  }
}
